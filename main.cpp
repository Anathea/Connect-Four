#include <iostream>
#include <vector>
#include "game.h"

using namespace std;

int main()
{
    /* How to Connect 4
     * The ideal would be a menu :
     * 1 - Play! (2 players)
     * 2 - Configure multiplayer
     * 3 - Configure gameboard size
     * 4 - How to play?
     *
     *
     *
     *
     *
     *
     */
    Game g;
    g.header();
//    g.changeSquareStatus(3, g.findFreeSquareInColumn(3), 2);
//    g.changeSquareStatus(3, g.findFreeSquareInColumn(3), 1);
//    g.changeSquareStatus(3, g.findFreeSquareInColumn(3), 1);
//    g.changeSquareStatus(3, g.findFreeSquareInColumn(3), 2);
//    g.changeSquareStatus(3, g.findFreeSquareInColumn(3), 1);
//    g.changeSquareStatus(3, g.findFreeSquareInColumn(3), 1);
//    g.changeSquareStatus(3, g.findFreeSquareInColumn(3), 1);

//    int players;
//    cout << "How many players are you? ";
//    cin >> players;
//    g.configMultiplayer(players);

    return 0;
}
